# Core
pyyaml==6.0.1
pydantic==2.11.4
typing-extensions>=4.12.2

# Web Framework
fastapi>=0.104.1
uvicorn[standard]>=0.23.2
jinja2>=3.1.4
python-multipart==0.0.6
aiofiles==23.1.0  # For static file serving

# LLM Interface
ollama # Unpinned, resolved to 0.4.8

# Async
aiohttp>=3.10.11 # Updated to meet auth0-python (indirect dependency) requirement

# Vector embeddings and search
numpy==1.24.3
# torch==1.13.1
transformers==4.31.0
sentence-transformers==2.2.2
huggingface-hub>=0.14.1,<0.17.0 # Constrained for sentence-transformers 2.2.2 compatibility

# IMPORTANT: FAISS-GPU Installation
# For GPU support (required for this application):
#   1. First install the exact dependencies in this order:
#      a) Create conda environment: conda create -n faiss_gpu_env2 python=3.10 -y
#      b) Install NumPy: pip install numpy==1.24.3
#      c) Install CUDA: conda install -c conda-forge cudatoolkit=11.8 -y
#      d) Install PyTorch: pip3 install torch==2.0.1+cu118 torchvision==0.15.2+cu118 --index-url https://download.pytorch.org/whl/cu118
#      e) Install FAISS-GPU: conda install -c conda-forge faiss-gpu=1.7.4 cudatoolkit=11.8 -y
#   2. Then install the remaining requirements with: pip install -r requirements.txt
#
# DO NOT have both faiss-cpu and faiss-gpu installed at the same time

# Tools
requests>=2.31.0
httpx>=0.27.0 # Added to meet ollama and chromadb (indirect dependency) requirements
pytz>=2023.3
python-dateutil>=2.8.2
beautifulsoup4>=4.12.2  # For web scraping
matplotlib>=3.7.2  # For visualization
reportlab>=4.0.4  # For PDF generation

# Web App
python-dotenv>=0.21.0
websockets>=12.0 # For potential real-time streaming

# Optional voice input
# openai-whisper>=20231117
# sounddevice>=0.4.6